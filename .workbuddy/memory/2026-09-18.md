# 2026-09-18 工作日志

## 期货研究数据每日自动更新（17:45 自动化，本次 18:01–18:05 执行）

- **期限结构**：`refresh_fundamentals.py` 全 59 品种刷新成功（term_date=2026-09-18），inv/basis/drivers/data_date 原样保留。
  - ⚠️ 修掉一个真 bug：`_fetch_market()` 直连分支写的是 `opener = urllib.request`（无 `open` 属性），导致不设代理时 59 品种全部静默失败（首次跑「成功 0 / 跳过 59」）。已改为恒用 `build_opener()`；该修复随当次 auto-push 进了 git（commit ad36bcb）。
  - 代理实况：FlClash GUI 未运行、`127.0.0.1:7890` closed（但 `clash-verge-service` 特权助手在跑，容易误判），而 eastmoney `push2delay` 直连 200 → 本次走直连，不强制代理。
- **资金流向**：`fetch_moneyflow.py` 成功，59 品种，asof 2026-09-18；净流入前三 AU +1883.19亿 / AG +788.78亿 / SN +274.71亿；净流出前三 SC -1914.13亿 / JM -976.05亿 / M -545.80亿。
- **产业利润快照**：35 品种全覆盖，顶层 asof=2026-09-18；本轮依据 9-16~9-18 公开源重写 28 条 note（其余保留旧 asof，保持诚实）。利润档位无变动；唯一数值更新：PVC 电石法平均成本 5392 → **5329.53 元/吨**（9-12 隆众/中财口径，毛利 -159.90）。
- **上线**：`sync_to_site.py --push` 成功（直连），commit 64e10f3 / 8d46cc0 两笔；patch 63 品种、term_date 当日 57 个；线上 profit_snapshot/moneyflow 均 asof 2026-09-18，REMOTE_V=202609181804.1。
- **状态**：`快照更新状态.json` 三段（profit / moneyflow / sync）全部 ok，无失败告警。

## 经验沉淀
- 已把三个坑写进技能 `futures-fundamentals-batch`（第 9~11 条）：refresh 直连 bug + 代理非必需、fetch_moneyflow 会顺带推送一次、profit 状态段需手动补写后再推。
