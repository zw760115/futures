# 2026-09-21 工作日志

## 每日收盘后数据自动更新（17:45 自动化任务）— 全部成功

**产出/改动**
- `~/Library/Application Support/期货服务/期货研究数据/*_库存基差期限结构.json`：59/59 品种 `term` 重建，`term_date=2026-09-21`；`inv/basis/drivers/data_date` 原样保留。合约数从"一键下载"留下的 6 条补齐到 10–35 条；`main_idx` 改为按**持仓量最大**修正（如 RB→rb2701、AG→ag2612、V→v2701）。
- `产业利润快照.json`：35/35 条 asof=2026-09-21，note/src 依 9-21 公开源重写；利润档位未动；cost 无同口径新值一律保留。
- `资金流向快照.json`：59 品种，asof 2026-09-21，净流入前三 BU/JM/SP、净流出前三 AU/AG/SC。
- 已 push GitHub Pages（commits da3974b / 55f1a70 / 4fd5903，REMOTE_V=202609211756）。

**重大技术变更：东财行情源迁移（本次核心）**
1. `push2delay / push2 / 82.push2 / 48.push2 / push2his / 1.push2 / 7.push2` **当日全部被重置**（TLS 握手成功 → 请求发出后空回复 `RemoteDisconnected`）；代理通/不通、沙箱内/外结果一致，成功率 0–5%。同机 `www.eastmoney.com`、`quote.eastmoney.com`、`futures.eastmoney.com` 仍 200 → 只有 push2 被掐，不是本机网络故障。
2. 改用 **`https://futsseapi.eastmoney.com/list/<mkt>`**（`Referer: https://futures.eastmoney.com/`；`pageSize=500&pageIndex=0` 一次返回该交易所全部合约；字段 `dm/name/p/o/h/l/zde/zdf/vol/cje/ccl/zjsj`）。6 个交易所 6/6 稳定。
3. `refresh_fundamentals.py`：新增 `_fetch_market_futsse()` 作首选源（push2 降为兜底）；新增 `_MARKET_CACHE` —— 原实现每个品种都重抓同一交易所（59 次请求），现在每次运行只需 6 次；`_build_term()` 持仓量列优先 `f78`、`main_idx` 按持仓量最大。
4. `fetch_moneyflow.py`：新增 `install_futsse_source(mod)`，**运行时 monkey-patch** 服务的 `_fetch_market`（不改服务源码），把 futsse 字段映射成 push2 名（f2/f5/f6/f15/f16/f18/f12/f14），`build_moneyflow()` 的 CLV 算法与网页口径零改动；另加 `ensure_snap_dirs(mod)` 把 Library 真源插进 `MF_SNAP_DIRS[0]`。
   - 原因：服务 `MF_SNAP_DIRS` 按脚本自身目录推导，而本脚本用 `~/Desktop/期货模板/期货行情服务.py` 软链路径加载服务 → 快照只写进 `~/Desktop/期货模板/期货研究数据/`，`sync_to_site` 只读 Library → 线上会停在旧副本。

**踩坑／待留意**
- "一键下载"链路（服务 `_build_result()`）只存 `rows[:6]`、`main_idx` 固定为 2 → 写进真源的是**截断版期限结构**（判据 `len(labels)==6 and main_idx==2`）。今天 58 个品种 term_date 已是当日但只有 6 条，SC 还停在 09-18。
- `快照更新状态.json` 的 `profit` 段没有脚本负责写，需显式 `notify.write_status('profit', ...)` 后**再**跑一次 push，否则线上 `data/update_status.json` 缺该段。
- 数据唯一真源仍是 `~/Library/Application Support/期货服务/期货研究数据/`（`~/Desktop/期货研究数据/` 已于 09-18 删除，脚本全部只认真源）。
- 已同步更新技能 `~/.workbuddy/skills/futures-fundamentals-batch/SKILL.md`（数据源结论、脚本改动、截断版判据、编号顺延）。
