# 2026-09-22 工作日志

## 期货研究数据每日自动更新（17:45 定时任务）
- 期限结构：`refresh_fundamentals.py` 59/59 成功，`term_date=2026-09-22`；`inv/basis/drivers/data_date` 原样保留。
- 资金流向：`fetch_moneyflow.py` 59 品种，asof 2026-09-22；净流入前三 SC / MA / LC，净流出前三 AU / AG / JM。
- 产业利润快照：35 品种 asof 刷新为 2026-09-22，note 依当日公开源全部重写；利润档位无变动，12 个 cost 全部保留原值。
- 推送：`sync_to_site.py --push` 共 3 次提交（08a24be / e74b37a / 603401c），REMOTE_V=202609221750，远端与本地 HEAD 一致。
- 状态：真源与站点 `快照更新状态.json` / `data/update_status.json` 三段（moneyflow / sync / profit）均 ok，无告警。

## 技术观察（已回写 skill）
- `refresh_fundamentals.py` 走 `futsseapi.eastmoney.com/list/<mkt>`，直连与代理 127.0.0.1:7890 均可用；本次带 `HTTP_PROXY/HTTPS_PROXY` 环境变量运行。
- `sync_to_site.py` 提交上去的 `sync.last_success` 天然滞后一次运行（脚本先复制状态、推送后才写自身状态段）；要贴近真实值需收尾再 push 一次。
- 校验远端推送结果必须带代理跑 `git ls-remote`（直连会 curl 28 超时 75s，与 push 是否成功无关）。
- 数据唯一真源：`~/Library/Application Support/期货服务/期货研究数据/`。
