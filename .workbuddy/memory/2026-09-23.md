# 2026-09-23 工作日志

## 17:45 自动化：期货研究数据每日更新（全部成功）

1. **期限结构刷新** `refresh_fundamentals.py`（走代理 7890）：59/59 成功，`term_date=2026-09-23`；
   `inv/basis/drivers/data_date` 原样保留（抽查 AU `data_date` 仍 2026-09-09）。
   合约数变动仅 J 32→33、LC 10→12、PT 5→6、PS 10→9，其余持平。
2. **资金流向** `fetch_moneyflow.py`：59 品种，asof 2026-09-23。
   净流入前三 RU +735.67亿 / BR +174.51亿 / NR +137.03亿；净流出前三 SC -803.06亿 / MA -506.55亿 / BU -362.08亿。
3. **产业利润快照**（一次性脚本，跑完即弃）：35 品种全部 asof=2026-09-23，note 依 9-23 公开源重写；
   利润档位无变动，12 个带 cost 品种全部保留原值（无同口径新值）。
   显式补写 `notify.write_status('profit', …)` 状态段。
4. **推送上线**：4 次提交，最终 `326069c`，`REMOTE_V=202609231823.1.2`，远端 HEAD 与本地一致。
5. **状态文件** `快照更新状态.json` 三段（moneyflow / sync / profit）全部 `ok`。

## 本次踩到的新坑（已写入 skill）

- **GitHub Pages 边缘缓存假象**：`data/profit_snapshot.json` 与 `data/update_status.json` push 后数分钟仍返回旧内容，
  `?v=<REMOTE_V>` 也穿不透；必须用 `?cb=<随机串>` 才能看到最新。而 `patch.json`/`moneyflow.json` 因为是前几次推送更新的，看起来是新的
  → 极易误判「利润快照没上线」。判据：先 `git show HEAD:data/x.json` 确认 git 里对，再用随机串请求。**不要重推**。
- **代理 TLS 瞬时抖动**：`sync_to_site.py --push` 经 7890 报 `SSL_ERROR_SYSCALL`，十几秒后同一路径 curl 200 → 直接重跑即成功，按重试处理。
- `notify.write_status()` 是 merge 语义，漏传 `src` 会沿用上一日的 src。
- 同分钟多次重推 → `REMOTE_V` 叠成 `202609231823.1.2`，无害。

## 环境

- 代理 7890 OPEN，出口 IP 66.90.99.210；东财 `futsseapi` 直连/代理均正常；`github.com` 直连 200（1.5s）。
- 工作区 `git status` 干净，无临时脚本残留。
