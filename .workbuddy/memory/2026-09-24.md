# 2026-09-24

## 每日 17:45 期货研究数据自动更新（全部成功）
- 期限结构：`refresh_fundamentals.py` 走代理 7890，59/59 成功，`term_date=2026-09-24`；`inv/basis/drivers/data_date` 保持原值（抽查 AU data_date 仍 2026-09-09，符合设计）。
- 资金流向：`fetch_moneyflow.py` 59 品种，asof 2026-09-24。净流入前三 SC +1703.35亿 / RU +738.80亿 / EG +692.35亿；净流出前三 AU -1603.74亿 / AG -333.15亿 / LH -231.02亿。
- 产业利润快照：35 品种 asof 全部更新为 2026-09-24，note 依 9-24 公开源重写；利润档位无变动（high 3 / mid 10 / low 22）；12 个带 cost 品种按规则保留原值（无同口径新值）。
- 推送：3 次提交，`REMOTE_V=202609241759.1`，远端 HEAD == 本地 HEAD == `66129fc`。线上 `profit_snapshot.json` / `update_status.json` / `moneyflow.json` / `patch.json` 均已为当日值。

## 本次得到的两条可复用结论（已回写 skill `futures-fundamentals-batch`）
1. **GitHub Pages 边缘缓存会同时打旧 `profit_snapshot.json` 与 `update_status.json`**，且刷新窗口可短至 20–40 秒 —— 判据永远是「`git show HEAD:data/...` + 带随机缓存键的线上取回」双确认，**不要重推**。`raw.githubusercontent.com` 直连本次空响应，不能当上游对照。
2. **数据目录实况**：除 Library 真源外，`~/Desktop/期货模板/期货研究数据/` 是一个真实目录（非软链），但只含 `fetch_moneyflow.py` 写的 `资金流向快照.json`；`产业利润快照.json` 与品种 JSON 只在 Library 真源。刷利润快照只写 Library 一份。

## 流程执行方式
- 利润快照刷新沿用一次性脚本：`json.load` → 按 code 更新 `asof/src/note` → `assert` 品种集合不变（35 个）→ 写 `_bak/产业利润快照.json.bak_20260924` → `json.dump(indent=1, ensure_ascii=False)` + 末尾换行 → 二次 `json.load` 校验。
- 脚本在 `/tmp` 起草并执行，跑完即删；仓库 `git status` 干净。
